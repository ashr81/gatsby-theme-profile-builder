import styled from '@emotion/styled';
import { Flex } from 'theme-ui';

const WelcomeTile = styled(Flex)`
  background-size: cover;
  transform-style: preserve-3d;
  transition: all 0.8s ease;
`

export default WelcomeTile;